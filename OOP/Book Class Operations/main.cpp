/*
 
 Ques: Create a class ‘book’ with name, price and number of pages as its attributes. The class should contain following member functions:

 countBooks(int price): This function will return count of all the books that have a price less than the given price.

 isBookPresent(string title): This will return a boolean value indicating whether any book with the given title exists.


 
 */

#include <iostream>
using namespace std;

class Book{
public:
    int name;
    int price;
    int noPage;
    
    
    
    
};

int main(int argc, const char * argv[]) {
    
}
